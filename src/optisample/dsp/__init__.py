"""Signal primitives (`spectral`, `resample`, `quantize`, `loop`) and the surrogate codec (`surrogate`).

No pass-through re-exports: import each helper from its owning module (per rule 4 in
``docs/architecture.md``).
"""
