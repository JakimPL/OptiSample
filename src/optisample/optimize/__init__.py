"""The allocation pipeline: `tasks`, `operating_points`, `velocity_map`, `knapsack`, `grouping/`,
`orchestrate`, `export`, `plans/`, and `report`.

No pass-through re-exports: import each type or function from its owning module (per rule 4 in
``docs/architecture.md``).
"""
