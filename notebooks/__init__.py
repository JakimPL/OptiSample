"""Marimo notebooks and their (thin, testable) helper modules.

The notebooks themselves (``*.py`` marimo apps) are intentionally thin and are *not* type-checked
or linted — all reusable logic lives in :mod:`notebooks.utils`, which is tested like any other code.
"""
